# v0.1.0 Manual Chrome Smoke Results

Date: 2026-09-20

Release target: `0.1.0`

Source commit: `f50ba77611b7e63717b65e6feabc2b38c4542800`
Build under test: production `dist/`

## Result policy

Every row below is explicitly recorded as `PASS`, `FAIL + issue`, or
`N/A + reason`. `N/A` in this document means that the test could not be
executed; it is not evidence that the product behavior passed. The release
candidate must not be accepted while the required manual rows remain N/A.

## Preconditions and evidence

| Check | Result | Evidence / reason |
| --- | --- | --- |
| Production build exists | PASS | `npm run build` completed before this run and the package contains `dist/manifest.json`. |
| Release package contract | PASS | `npm run release:check` passed on source `f50ba77`; version `0.1.0`, 18 files. |
| Official Chrome available | PASS | `/Applications/Google Chrome.app` was available; the direct profile reported Chrome `153.0.8010.52`. |
| Playwright isolated context can load the extension | FAIL + environment limitation | The configured headless and visible sessions launched with the production path but exposed no extension service worker. The isolated context also rejected `chrome://extensions` navigation. |
| Official Chrome command-line unpacked load | FAIL + environment limitation | Chrome logged `--disable-extensions-except is not allowed in Google Chrome, ignoring`; a direct profile with `--load-extension` exposed only built-in component extensions, not this package. |
| Exact `dist/` loaded through Chrome's extension UI | N/A + environment limitation | Completing the native Load Unpacked picker requires macOS Accessibility/native-dialog control, which is unavailable in this session. No Chrome profile used for this attempt contained the LeetCode Visualizer extension. |
| Active authenticated LeetCode session | N/A + environment limitation | No credentials or existing browser cookies were used. Without the loaded extension and a user-authorized LeetCode session, the live editor/testcase matrix cannot be run. |

The evidence above is why this task is `BLOCKED/PENDING`, rather than a
manual smoke pass. No production behavior is inferred from the static build
checks.

## Lifecycle

| Check | Result | Reason |
| --- | --- | --- |
| Fresh unpacked install | N/A + reason | The exact `dist/` directory was not installed through Chrome's native picker. |
| Toolbar click opens Side Panel | N/A + reason | The extension was not present in the Chrome toolbar. |
| Close and reopen Side Panel | N/A + reason | Requires a real Side Panel opened by the extension action. |
| Extension reload | N/A + reason | Requires an installed unpacked extension and Chrome's Reload control. |
| LeetCode page reload | N/A + reason | No extension-backed LeetCode page was available. |
| LeetCode tab A → tab B | N/A + reason | Requires two live LeetCode tabs and extension ownership checks. |
| LeetCode → non-LeetCode → LeetCode | N/A + reason | Requires navigation with the content script installed. |

## Public-user states

| Check | Result | Reason |
| --- | --- | --- |
| No active LeetCode page | N/A + reason | Requires the Side Panel in a real installed extension context. |
| Editor not mounted | N/A + reason | Requires a live LeetCode page before editor mount. |
| Python editor | N/A + reason | Requires a live LeetCode editor and extension bridge. |
| Unsupported language | N/A + reason | Requires changing a live LeetCode editor to a non-Python language. |
| Empty editor | N/A + reason | Requires a live editor with empty source. |
| Syntax error | N/A + reason | Requires executing a syntax-invalid Python fixture in the real UI. |
| Missing testcase | N/A + reason | Requires a live problem without a usable testcase. |
| Multiple Cases | N/A + reason | Requires a live testcase panel containing multiple cases. |
| Connection failure / reload recovery | N/A + reason | Requires a live page and an observable bridge/reload interruption. |

## Fixed problem set

### 704 Binary Search

| Check | Result | Reason |
| --- | --- | --- |
| Live sync | N/A + reason | No live LeetCode editor or content-script bridge was available. |
| List visualization | N/A + reason | Requires the real Side Panel runtime. |
| Decision Evidence | N/A + reason | Requires a completed 704 execution trace. |
| Mutation evidence | N/A + reason | Requires a completed 704 execution trace. |
| Cross-Run Behavioral Diff | N/A + reason | Requires two completed 704 runs in the Side Panel. |
| Inspect current | N/A + reason | Requires a rendered 704 trace and interactive navigation. |

### 206 Reverse Linked List

| Check | Result | Reason |
| --- | --- | --- |
| Linked-list state and reference behavior | N/A + reason | Requires a live 206 execution trace in the installed extension. |

### 104 Maximum Depth of Binary Tree

| Check | Result | Reason |
| --- | --- | --- |
| Testcase decoding | N/A + reason | Requires a live 104 testcase. |
| Tree visualization | N/A + reason | Requires a completed 104 execution trace. |
| Recursion and Call Path | N/A + reason | Requires a completed 104 recursive trace. |
| Call Tree and returns | N/A + reason | Requires interactive Call Tree navigation in the Side Panel. |

### Matrix / DP fixture

| Check | Result | Reason |
| --- | --- | --- |
| Rectangular grid fixture | N/A + reason | Requires a known-good live matrix problem and execution trace. |
| Matrix viewport and focus | N/A + reason | Requires the rendered Side Panel viewport. |
| Supported overlays/path | N/A + reason | Requires a completed matrix trace. |

### Graph fixture

| Check | Result | Reason |
| --- | --- | --- |
| `Node(val, neighbors)` fixture | N/A + reason | Requires a live graph problem and execution trace. |
| Components and edges | N/A + reason | Requires the graph renderer in the real Side Panel. |
| Inspection | N/A + reason | Requires interactive graph-state inspection. |

## Failure modes

| Check | Result | Reason |
| --- | --- | --- |
| Runtime exception | N/A + reason | Requires executing an exception fixture through the extension worker. |
| Trace limit | N/A + reason | Requires a fixture that reaches the configured trace limit. |
| Hard timeout | N/A + reason | Requires a long-running live execution. |
| Captured prefix preserved | N/A + reason | Requires observing a partial trace after a failure/limit. |
| Failure-First remains optional | N/A + reason | Requires the rendered failure controls and a failing trace. |
| Local timeout is not described as LeetCode TLE | N/A + reason | Requires observing the runtime status copy in the live UI. |

## Navigation

| Check | Result | Reason |
| --- | --- | --- |
| Previous | N/A + reason | Requires an interactive trace with more than one event. |
| Next | N/A + reason | Requires an interactive trace with more than one event. |
| Play / Pause | N/A + reason | Requires the live timeline controls. |
| Timeline scrub | N/A + reason | Requires a rendered trace timeline. |
| Call Tree navigation | N/A + reason | Requires a completed recursive trace. |
| Behavioral Diff → Inspect current | N/A + reason | Requires two completed runs and the diff UI. |

## Release decision

`BLOCKED/PENDING`. The production build and static release contract pass, but
the required manual Chrome matrix has no executable PASS rows because this
environment could not install the unpacked extension through official Chrome
or provide an authenticated LeetCode UI. The next run must use Chrome's
Developer Mode → Load unpacked flow for `dist/`, a user-authorized LeetCode
session, and the fixed fixtures above; then replace each N/A with `PASS` or
`FAIL + issue` and attach only real UI screenshots.
