{
  "name": "leetcode-python-execution-visualizer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build && vite build --config vite.content.config.ts && vite build --config vite.page-bridge.config.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "pyodide": "^0.29.3"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.326",
    "@types/node": "^22.13.10",
    "jsdom": "^26.1.0",
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "vitest": "^3.2.4"
  }
}
