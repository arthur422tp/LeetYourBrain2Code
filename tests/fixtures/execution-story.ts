import type { BuildControlFlowUiModelInput } from "../../src/core/execution-story";
import { buildControlFlowEvidence } from "../../src/core/control-flow-interpreter";
import type { LoopIterationEvidence } from "../../src/shared/control-flow-types";
export function storyInput(): BuildControlFlowUiModelInput {
  const context = { loopStack: [{ loopId: "f1", iteration: 2 }, { loopId: "f2", iteration: 4 }] };
  const iterations: LoopIterationEvidence[] = [3, 4].map((iteration, i) => ({ frameId: 1, loopId: "f2", iteration, context: { loopStack: [context.loopStack[0]!, { loopId: "f2", iteration }] }, anchorStepStart: 10 + i * 3, anchorStepEnd: 12 + i * 3, bindings: [{ name: "x", value: { type: "int", value: "7" } }], status: i ? "broke" : "completed" }));
  return { step: 14, frameId: 1, sourceCode: "for row in rows:\n    for x in row:\n        break", plan: { version: 1, loops: [{ loopId: "f2", kind: "for", span: { line: 2, column: 4, endLine: 3, endColumn: 13 } }], transfers: [{ transferId: "t1", kind: "break", span: { line: 3, column: 8, endLine: 3, endColumn: 13 } }] }, controlFlow: { ...buildControlFlowEvidence(undefined, [], []), iterations, iterationsByActivation: new Map([["1:f1#2>f2", iterations]]), contextByStep: new Map([[14, context]]), actions: [{ actionId: "a1", transferId: "t1", kind: "break", frameId: 1, context, anchorStepObserved: 14, anchorStepResolved: 15, status: "committed" }], loopExits: [{ frameId: 1, loopId: "f2", loopKind: "for", context: { loopStack: [context.loopStack[0]!] }, anchorStep: 16, reason: "break" }] }, decisionEvidence: new Map([[13, { anchorStep: 13, frameId: 1, context, siteId: "d1", occurrence: 4, status: "completed", condition: { conditionId: "c1", kind: "comparison", source: "x < 0", status: "evaluated", truth: false, operands: [], children: [] }, structureReferences: [] }]]), decisionChains: [] };
}
